// localStorage.setItem("clé", "valeur") on peu appeler la clé comme on veut
//pour recuperer la valeur de la clé, c'est: nom.value
// localStorage.setItem("clé")
// localStorage.clear();
//JSON.stringify(objet) transforme l'objet mis en parametre en 'string'
//JSON.parse(string) transforme le string mis en parametre en 'objet'

/*const cart = [];

function recupElementStorage() {
  const nbreElement = localStorage.length;
  for (let i = 0; i < nbreElement; i++) {
    const element = localStorage.getItem(localStorage.key(i));
    const ajoutElement = JSON.parse(element);
    cart.push(ajoutElement);
  }
}
recupElementStorage();*/
